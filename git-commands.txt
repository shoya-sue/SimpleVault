git add docs/smart_contract/README.md docs/gui/README.md
git commit -m "docs: スマートコントラクトとGUIの詳細設計ドキュメントを追加"

git add cleanup.sh cleanup.bat
git commit -m "chore: プロジェクト構造クリーンアップスクリプトを追加"

# 次のステップ: GUIの実装
# 以下のコマンドはGUI実装後に実行

# cd gui
# mkdir -p src/{components,hooks,pages,utils}
# touch src/components/{DepositForm.tsx,WithdrawForm.tsx,BalanceDisplay.tsx,WalletConnect.tsx}
# touch src/pages/index.tsx
# touch src/hooks/useWallet.ts src/hooks/useVault.ts
# touch src/utils/api.ts src/utils/constants.ts
# npm init -y
# npm install react react-dom next @solana/web3.js @solana/wallet-adapter-react @solana/wallet-adapter-wallets @coral-xyz/anchor
# npm install --save-dev typescript @types/react @types/node
# git add .
# git commit -m "feat: GUIの基本構造とファイル追加" 